{
  "name": "omnicloud-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare:legacy": "node scripts/prepare-legacy.mjs",
    "dev": "npm run prepare:legacy && wrangler dev",
    "deploy": "npm run prepare:legacy && wrangler deploy",
    "types": "wrangler types",
    "db:apply": "node scripts/db-apply.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1065.0",
    "@aws-sdk/lib-storage": "^3.1065.0",
    "@neondatabase/serverless": "^1.1.0",
    "dropbox": "^10.34.0",
    "hono": "^4.12.33",
    "megajs": "^1.3.10"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
